#include <regex>
#include <iostream>
using namespace std;

namespace Day3
{
    inline void Run(string inputData)
    {
        int calibrationSum = 0;

        int size = inputData.length();
        string* row1 = new string[100];
        string* row2 = new string[100];
        string* row3 = new string[100];

        int inputRow = 0;

        for (int i = 0; i < inputRow; i++)
        {

            //total += stoi(tempString);
        }

        //std::cout << total;
    }
}